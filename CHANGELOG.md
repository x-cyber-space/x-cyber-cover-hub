# 更新日志

本文件记录项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/spec/v2.0.0.html)。

## [未发布]

首个版本。项目从姊妹项目 `x-cyber-lrc-hub` 中独立出来，因为封面与歌词在**匹配维度、缓存形态、协议身份**三件事上都不同（详见 `docs/SPEC.md` 第 2 节）。

### 新增

- `GET /api/cover`：按 `artist_name` + `album_name` 解析专辑封面，默认返回图片字节。
- `?format=json`：返回元数据（含 `artworkUrl` 与指向本服务的 `imageUrl`）。
- `?size=`：请求任意边长（64–3000），两个上游源都能从一张原图给出不同分辨率。
- `ETag` + `If-None-Match`：图片命中返回 `304`，不重传字节。
- `-cache-ttl`：缓存有效期，默认 90 天，`0` 表示永不过期。
- `-size`：未带 `?size` 时的默认边长，默认 600（即写入 ID3/FLAC 标签所需的尺寸）。
- 两个上游源：**iTunes 为主**（免费、无 key、无防盗链、URL 内可改分辨率），**网易云为备**（覆盖中文曲库）。
- 缓存命中会用**与冷启动相同的谓词**复核。
- 全部基础设施：CI（含容器构建 + 启动冒烟测试）、CodeQL、依赖审查、OpenSSF Scorecard、Dependabot、GoReleaser 发布、多架构 GHCR 镜像。

### 设计要点

- **缓存键是 (歌手, 专辑, 边长)，不是歌曲。** 消费方原本按 `cover_<songId>.jpg` 缓存，一张 12 首的专辑会抓 12 次、存 12 份相同的图。这是本项目存在的首要理由。
- **先匹配再取图。** 消费方原本 `limit=1` 盲取第一条搜索结果；封面会被永久写进音频文件，错了要人工改。
- **专辑权重（60）高于歌手（40）。** 一个歌手有十几张封面，认错专辑才是要防的失败模式。
- **兜底是遍历而非重试。** iTunes 经常匹配到专辑但没有 artwork，而网易云同一条记录有；所以按序逐个尝试下载，直到拿到图。
- **`503` 与 `404` 分开。** 上游故障不该被伪装成"没有这张专辑"。

### 已知边界

- 未传 `album_name` 时只能退化为 `MatchRelaxed`，且不查缓存（没有专辑就没有身份）。
- 不做图片解码、不校验实际尺寸、不转码。
- 不做负缓存。

[未发布]: https://github.com/x-cyber-space/x-cyber-cover-hub/commits/main
