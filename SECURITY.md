# 安全策略

## 报告安全漏洞

请**私下**报告安全问题，不要开公开的 issue。

用本仓库的 GitHub [私密漏洞报告][report] 功能。如果该功能不可用，直接联系维护者。

请包含以下内容：

- 问题是什么、位于哪里（文件、端点、命令行参数）
- 如何复现
- 你认为的影响
- 如果有修复建议，也一并给出

一般会在几天内确认收到。这是一个由志愿者维护的小项目，公开披露前请留出合理的处理时间。

[report]: https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability

## 支持的版本

只有 `main` 分支上的最新版本会收到修复。

## 范围与威胁模型

`x-cyber-cover-hub` 面向家庭网络运行（通常挂在 NAS 或路由器后面），并且**不带任何认证**。评估任何报告时，这一点都是前提。

### 属于受理范围

- 任何能让请求逃出预期 HTTP 表面的问题：路径穿越、向 SQLite 缓存注入、请求走私。
- 单个请求即可导致的 memory / goroutine 耗尽。本项目**下载并缓存图片**，所以这条比同类服务更要紧：无界的读取、无界的并发扇出、缺失的超时。
- 通过响应或日志泄漏本地文件或环境变量。
- 把本服务变成任意 URL 的取回器（SSRF）。上游 URL 全部由服务端按固定模板构造，**不应存在任何由请求参数直接决定的抓取地址**——若发现可以，那是一个真漏洞。

### 已知且有意为之

- **没有认证。** 任何能连到端口的人都能使用本服务。**不要把它暴露到公网**；要么放在带认证的反向代理后面，要么就只留在局域网里。
- **`Access-Control-Allow-Origin: *`。** 浏览器端的播放器要能访问自建实例就必须允许这个头。但请注意它同时意味着：**你访问的任何网页都可以向你的实例发起请求**，而这些请求会以你的 IP 发出。如果在意这点，用反向代理限制来源。
- **第三方图片是联网抓取的。** 抓到的图片只作为字节返回、从不执行，但其准确性与版权状态都未经校验。
- **本地缓存保存图片字节。** 这是它能绕过防盗链、并为同一专辑的多首歌复用一份数据的原因，但确实意味着磁盘上留有一份第三方素材。

## 密钥处理

本服务不保存任何凭据，也不需要任何 API key——两个上游源都是公开接口。唯一的持久化状态是 `-cache` 指定路径上的一个 SQLite 文件，内容为专辑元数据与图片字节。
